import java.text.DecimalFormat;

public class SandWich {

    private String Bread;
    private String Meat;
    private String Cheese;
	private double Price;



private String chooseBread()
{
	String [] Bread = {"Ham", "Roast Beef", "Chicken", "Turkey", "Tuna Salad"};
	int choice = (int)(Math.random()*meats.length);
	return bread[choice];
}


private String chooseMeat()
{
	String [] meats = {"Ham", "Roast Beef", "Chicken", "Turkey", "Tuna Salad"};
	int choice = (int)(Math.random()*meats.length);
	return meats[choice];
}

p

private String chooseCheese()
{
	String [] Cheese = {"Ham", "Roast Beef", "Chicken", "Turkey", "Tuna Salad"};
	int choice = (int)(Math.random()*meats.length);
	return Cheese[choice];
}


private double Price()
{
	Price = 2.50;

}
 
}
